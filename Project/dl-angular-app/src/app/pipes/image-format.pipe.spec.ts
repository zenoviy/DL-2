import { ImageFormatPipe } from './image-format.pipe';

describe('ImageFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
