import { LinkNameFormatPipe } from './link-name-format.pipe';

describe('LinkNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
